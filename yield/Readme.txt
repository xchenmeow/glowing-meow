1. Run Terminal
2. Change directory (cd) to "enum-0.4.4" folder.
3. Run "python setup.py install"
4. The program should run automatically.
5. Once it's installed, user should be able to "import enum" within a python script.